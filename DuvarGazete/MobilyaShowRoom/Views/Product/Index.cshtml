@model List<MobilyaShowRoom.Models.Product>
@{
    Layout = "_Layout";
}

@*<table class="table table-dark w-50 mx-auto">
    <thead>
        <tr>
            <th>#</th>
            <th>Product Name</th>
            <th>İşlem</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var product in Model)
        {
            <tr>
                <td>@product.ID</td>
                <td>@product.ProductName</td>
                <td> <a href="product/getproductdetail/@product.ID"> Detay</a></td>
            </tr>

        }
    </tbody>
</table>*@
<div class="container "
     >
    <div class="row justify-content-center "  >
        @foreach (var product in Model)
        {
            <div class="card text-center bg-danger mx-2 col-5 my-2  ">
                <div class="card-header">
                    @product.ID
                </div>
                <div class="card-body bg-white">
                    <h5 class="card-title">@product.ID</h5>
                    <p class="card-text">@product.ProductName</p>



                    <a href="product/getproductdetail/@product.ID"> Detay</a>
                </div>
                <div class="card-footer text-muted">
                    son 2 ürün
                </div>
            </div>
        }
    </div>
</div>
